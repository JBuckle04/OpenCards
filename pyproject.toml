[project]
name = "open-cards"
version = "0.1.0"
description = "A local JSON flashcard reviewer with Anki-style grading and LLM-friendly deck workflows."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
open-cards = "app:main"

[tool.pytest.ini_options]
python_files = ["test_*.py"]
